# Retire Compare; rename Breakdowns to Segments — 25 September

Owner decision: the Compare page is redundant. A single-country split is what Breakdowns already does. Delete Compare entirely. Keep Breakdowns **exactly as it behaves today** and rename it **Segments** in everything a user sees.

## How to work

- Read `CLAUDE.md` once. Locate code with `grep`. Read only the relevant ranges and don't re-read files after editing.
- **Don't rename internal identifiers or files.** `BreakdownsView.tsx`, `parseBreakdownsSearch`, `meta.breakdowns` and the rest stay as they are. Only user-facing text and the URL change.
- No behaviour changes to Breakdowns. No data rebuild.
- Branch `claude/segments` from `main`. If the `claude/change-page` PR is still open, branch from it instead and say so in the PR.
- One conventional commit per task. Include this prompt file in the first one.
- Open one PR. Don't merge, tag or deploy.
- End with one line per task.

## Tasks

### 1 · Delete Compare

**Remove:**
- the nav item (`nav.ts:10` and its entry near line 33);
- the route (`router.tsx` ~99–105 and its entry in the route tree);
- `views/CompareView.tsx`, `views/compareRows.ts`, `charts/CompareDomains.tsx` and their styles;
- the compare search codec in `state/search.ts` (`parseCompareSearch`, `compareSearchParams`, defaults);
- their tests: `__tests__/compare.test.tsx`, and the Compare parts of `charts.test.tsx` and `search.test.ts`.

**Also delete** any helper, style or token that only Compare used. Grep after deleting and remove anything left unreferenced; the typecheck and ESLint unused rules will flag it.

**Leave the server alone.** No change to the API or static exporter, even if something there only served Compare.

**Redirect:** `/compare` becomes a redirect to `/segments`. Carry `outcome` and `wave` across if present and valid; drop every other param silently, with no invalid-param notice.

### 2 · Rename Breakdowns to Segments (user-facing only)

- **Route path:** `/segments`. `/breakdowns` redirects there, keeping all search params, so shared links keep working.
- **Nav label:** "Segments".
- **Page text:**
  - `titled('Segments')`, so `document.title` reads "Segments — Flourish Atlas";
  - the visible and visually-hidden `<h2>` (`BreakdownsView.tsx:132, 140, 310`);
  - any lede or hint text that names the page.
- **Download file names:** if the view part says "breakdowns", it now says "segments" (grep the export helper and `view:` in `BreakdownsView.tsx`). Keep the server CSV name in `routes/export.py` identical to the client's.
- **Other pages:** grep the user-facing strings `Breakdowns` and `Compare` in `src` (e.g. Methods, the Codebook "Chart this" targets, empty states), and point them at Segments or remove them.

### 3 · Docs

- **README:**
  - Drop the Compare feature paragraph (~line 17).
  - Rename Breakdowns to Segments in the feature text (~line 26) and in the repo-map line (~line 85).
  - Leave the phase history table as it is: it is history.
- **CLAUDE.md:** the repo-map views list says "Segments (`BreakdownsView`)".
- **ADR-0017** (MADR, brief): Compare retired as redundant with a single-country split; Breakdowns renamed Segments; the old URLs redirect.

## Verification

- **Per task:** run only the affected tests, e.g. `pnpm -C apps/web vitest run <files>`.
- **Router test:** `/compare?outcome=sfi&countries=1,2` lands on `/segments?outcome=sfi`, and `/breakdowns?by=gender` lands on `/segments?by=gender`.
- Update any Playwright journey that clicks "Breakdowns" or "Compare".
- **End, once:** `make lint typecheck`, `pnpm -C apps/web vitest run`, `pnpm -C apps/web budget` (it should shrink), and the Playwright journeys. Then push.
