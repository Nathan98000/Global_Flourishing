# Placeholder: the deploy workflow copies flourish.duckdb + manifest.json
# here from the private bucket before `docker build`; CI builds with the
# directory empty and the image reports data=absent (ADR-0007).
