// Placeholder until the rendered docs/METHODS.md ships in the last PR of
// this stack.

export function MethodsView() {
  return (
    <section>
      <h2>Methods</h2>
      <p>
        Every estimate shows its survey weight, unweighted n, and a design-based confidence
        interval; cells with n &lt; 50 are suppressed. Associations, not causes. The full write-up
        (rendered from docs/METHODS.md) lands later in this stack.
      </p>
    </section>
  )
}
